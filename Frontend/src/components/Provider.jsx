import React, { useState } from 'react'
import ThemeContext from './ThemeContext';
ThemeContext
const Provider = () => {
    const [mode,setMode] = useState(1);
  return (
    <themeContext.Provider  valu>

    </themeContext.Provider>
  )
}

export default Provider
